Your API key lives in Postiz under **Settings → Developers → Public API**.
Click the reveal icon to show it, then copy. You can rotate it from the same
place, rotating immediately invalidates the old key, so update anything using
it (scripts, n8n, MCP clients, the CLI) at the same time.
